import React from "react";

const FooterDiscText = ({ text }) => {
  return (
    <span className="uppercase text-footer_disc_text font-light leading-[1.1] text-ibm inline-block">
      {text}
    </span>
  );
};

export default FooterDiscText;
